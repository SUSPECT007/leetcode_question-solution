class Solution {
public:
    int maximumCount(vector<int>& nums) {
        int n=nums.size(),c0=0,c1=0;
        if(nums[0]>0 || nums[n-1]<0) return n;
        for(int x:nums){
            if(x<0) ++c0;
            else if(x>0) ++c1;
        }
        return max(c0,c1);
    }
};
