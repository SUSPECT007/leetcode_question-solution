class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int> ans;
        int x;
        for(int i=0;i<nums.size();++i){
            x=nums[i]*nums[i];
            ans.push_back(x);
        }
        sort(ans.begin(),ans.end());
        return ans;
    }
};
