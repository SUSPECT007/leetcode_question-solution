class Solution {
public:
    int findSpecialInteger(vector<int>& arr) {
        int ans,n=arr.size(),count=1,s=n/4;
        for(int i=1;i<n;++i){
            if (arr[i]==arr[i-1]) ++count;
            else count=1;
            if (count>s) return arr[i];
        }
        return arr[0];


    }
};
