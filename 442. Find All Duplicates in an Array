class Solution {
public:
    vector<int> findDuplicates(vector<int>& nums) {
        int n=nums.size();
        vector<int> v(n+1,0),ans;
        for(int x:nums) ++v[x];
        for(int i=1;i<n+1;++i)
            if (v[i]==2) ans.push_back(i);
        return ans;
    }
};
